* 10.07 Org Zhixing Multi-Theme RFC and Design

This is the canonical RFC and specification for multiple Org Zhixing themes,
custom theme authoring, runtime visual variants, and framework adapters.

** RFC Record

- Status :: Accepted
- Date :: 2026-07-12
- Decision :: Themes are versioned npm packages with one
  =package.json#orgZhixing= manifest.  Stable semantic slots and typed variants
  form the customization API.  Renderer adapters consume a portable site model.

** Context

Org Zhixing must let site authors install several themes and select one through
=org-zhixing.toml=.  The workspace must maintain several built-in themes, users
must be able to create normal package-based custom themes, and modern frameworks
such as Next.js must be able to render the same theme identities without owning
the theme protocol.

Hugo demonstrates useful theme composition, but unrestricted path shadowing
makes internal files an implicit API.  Org Zhixing instead combines package
distribution, explicit compatibility, semantic slots, and design-token variants.

** Decision and Boundaries

#+begin_example
org-zhixing content model
          |
portable theme definition
          |
framework renderer binding
          |
Next.js / React SPA / future adapters
#+end_example

A complete theme owns structure and components.  A visual variant changes only
declared tokens and variant-safe styles.  Site authors select complete themes at
build time.  Visitors may switch variants at runtime; switching complete themes
at runtime is out of scope.

Built-in, local, and published themes use the same manifest and contract suite.
Configuration selects themes; =package.json=, workspaces, and the lockfile install
and version them.  There is no separate =org-zhixing-theme.toml= manifest.

** Workspace Specification

#+begin_example
.data/org-zhixing/
  package.json
  packages/
    core/package.json
    theme-runtime/package.json
    theme-tooling/package.json
  themes/
    elegant-blog/package.json
    minimal-notes/package.json
    research-garden/package.json
  adapters/
    next/package.json
    react-spa/package.json
#+end_example

The root declares =packages/*=, =themes/*=, and =adapters/*= as workspaces.  Every
workspace has its own =package.json=.  External themes have the same shape whether
linked locally or installed from npm.

** Theme Package Specification

#+begin_src json
{
  "name": "@org-zhixing/theme-minimal-notes",
  "version": "0.1.0",
  "type": "module",
  "exports": {
    ".": "./dist/index.js",
    "./react": "./dist/react.js"
  },
  "peerDependencies": {
    "@org-zhixing/core": "^0.4.0",
    "react": "^19.0.0"
  },
  "orgZhixing": {
    "schemaVersion": 1,
    "id": "minimal-notes",
    "displayName": "Minimal Notes",
    "engine": ">=0.4 <0.5",
    "defaultVariant": "light",
    "variants": ["light", "solarized-dark"],
    "capabilities": ["blog", "agenda", "search"],
    "publicSlots": [
      {
        "id": "site-header",
        "strategies": ["wrap", "replace"],
        "runtime": "universal",
        "stability": "stable"
      }
    ],
    "renderers": {
      "react": {"export": "./react", "serverComponents": true}
    },
    "renderModes": ["static", "ssr", "streaming"],
    "configSchema": "./theme-config.schema.json",
    "preview": "./assets/preview.webp"
  }
}
#+end_src

=schemaVersion= versions the manifest.  =id= is the configuration name.
=engine= declares core compatibility.  =capabilities= declares site surfaces.
=publicSlots= is the stable customization surface.  =renderers= maps theme
identity to framework bindings.  =renderModes= declares deployment support.
=configSchema= validates theme-owned options.

** Site Configuration Specification

#+begin_src toml
theme = "minimal-notes"
theme_variant = "solarized-dark"

[theme_config]
show_toc = true
density = "compact"
#+end_src

=theme= selects structure.  =theme_variant= selects a compatible visual overlay.
=[theme_config]= is validated by the selected package.  The legacy
=[theme].name= shape receives a bounded =org-zhixing config migrate= route and is
then removed with its parser branch, tests, and documentation.

** Discovery and Registry Specification

Discovery follows explicit workspace and installed dependencies, not arbitrary
directory scanning:

#+begin_example
dependencies -> orgZhixing manifests -> validation -> generated registry
             -> org-zhixing.toml selection -> adapter build
#+end_example

The generated registry keeps a metadata-only catalog of every discovered theme,
but emits a runtime import for only the theme selected by =org-zhixing.toml=:

#+begin_src typescript
import selectedTheme from "@org-zhixing/theme-elegant-blog";

export const generatedThemeCatalog = [
  { id: "elegant-blog", package: "@org-zhixing/theme-elegant-blog" },
  { id: "minimal-notes", package: "@org-zhixing/theme-minimal-notes" },
] as const;

export const generatedThemes = [selectedTheme] as const;
#+end_src

Only the selected structural theme enters production output.  Duplicate IDs are
errors naming both packages; local packages do not silently shadow remote ones.

** Portable Platform Specification

The core exposes a serializable site model:

#+begin_src typescript
interface ZhixingSiteModel {
  site: SiteMetadata;
  navigation: NavigationModel;
  pages: PageProjection[];
  assets: ThemeAssetManifest;
}
#+end_src

Themes consume a narrow platform interface:

#+begin_src typescript
interface ThemePlatform {
  Link: ThemeLinkComponent;
  Image: ThemeImageComponent;
  navigate?: NavigateFunction;
  capabilities: ReadonlySet<PlatformCapability>;
}
#+end_src

Routing, optimized images, request APIs, and caching belong to adapters.  Themes
declare optional capabilities instead of importing framework-private APIs.

** Renderer Adapter Specification

The initial packages are =@org-zhixing/core=,
=@org-zhixing/theme-runtime=, =@org-zhixing/adapter-react-spa=, and
=@org-zhixing/adapter-next=.

React is the first renderer binding.  The Next.js adapter maps Org projections
to App Router routes, supplies static parameters and metadata, maps semantic
links and images, and renders static structure in Server Components by default.
Search and variant controls remain small client leaves.  Static export is the
first target; SSR, streaming, and revalidation require declared capabilities.
Themes do not directly call request-time APIs such as cookies or headers.

Public components declare =server=, =client=, or =universal= runtime.  Layouts,
articles, navigation, and tables of contents default to =server=.  Interactive
controls are leaf-level =client= islands.

** Stable Slot Specification

#+begin_src typescript
type StableThemeSlot =
  | "site-header"
  | "site-footer"
  | "sidebar"
  | "article-before"
  | "article-after"
  | "agenda-toolbar";
#+end_src

Each slot declares versioned props, =replace= and/or =wrap= behavior, runtime,
and =stable=, =experimental=, or =internal= status.  Only stable slots receive
semantic-version guarantees.  Internal components and file paths are not public
override surfaces.

** Token and Variant Specification

Tokens use typed semantic names with CSS custom-property output for canvas,
surface, text, accent, typography, and spacing roles.  A variant may override
declared tokens and variant-safe component parameters.  It may not replace
layout structure or content projection.

** Custom Theme Workflow

#+begin_example
org-zhixing theme create my-theme
org-zhixing theme create my-variant --extends minimal-notes
org-zhixing theme check ./themes/my-theme
org-zhixing theme preview ./themes/my-theme
#+end_example

The generated package contains =package.json=, =src/index.ts=, a configuration
schema, preview asset, and contract test.  Authors choose a React server-first,
React universal, or tokens-only template and supported adapter fixtures.

** Diagnostics

| Code       | Meaning                                   |
|------------+-------------------------------------------|
| THEME-E001 | unknown theme ID                          |
| THEME-E002 | duplicate theme ID                        |
| THEME-E003 | incompatible core version                 |
| THEME-E004 | missing renderer for selected adapter     |
| THEME-E005 | unknown variant                           |
| THEME-E006 | invalid =[theme_config]=                  |
| THEME-E007 | unresolved package export                 |
| THEME-E008 | server/client boundary violation          |
| THEME-E009 | invalid manifest or out-of-root asset      |
| THEME-E010 | performance budget exceeded               |
| THEME-E011 | invalid or legacy site theme configuration |

Successful builds emit a compact receipt containing theme package and version,
variant, adapter, render mode, client JavaScript size, and route count.

** Performance Requirements

- Unselected complete themes do not enter production output.
- Variant switching does not load a second structural theme.
- Static themes do not accidentally opt into request-time rendering.
- Client JavaScript and CSS have explicit budgets.
- Server-only dependencies never enter client bundles.
- Adapter receipts report route modes, client islands, and asset sizes.

** Test Requirements

Each theme verifies its manifest and exports; IDs, variants, capabilities, and
slots; renderer bindings against supported adapter fixtures; server/client
boundaries; configuration fixtures; resource containment; accessibility;
visual regression; bundle budgets; and representative Org content.

Repository tests cover workspace and external discovery, duplicate rejection,
theme selection, runtime variants, unselected-bundle exclusion, current SPA and
Next.js fixtures, scaffolding, and legacy configuration migration.

** Delivery Stages

*** Stage 1: Workspace Theme Packages

Define =package.json#orgZhixing=, migrate =elegant-blog= to an ordinary theme
package, generate the registry, and preserve behavior through the SPA adapter.

*** Stage 2: Canonical Configuration

Add top-level theme configuration, schema validation, diagnostics, receipts, and
the bounded legacy migration route.

*** Stage 3: Customization Surface

Add stable slots, runtime declarations, typed variants, authoring commands,
accessibility fixtures, visual tests, and bundle budgets.

*** Stage 4: Next-Generation Adapters

Finalize portable contracts, implement the Next.js adapter, verify static export
and Server Components, and document the adapter protocol for future frameworks.

** Non-Goals

- Visitor switching between complete structural themes.
- Package installation through =org-zhixing.toml=.
- Arbitrary file shadowing.
- Permanent dual configuration routes.
- Requiring every theme to support every framework.
- Migrating the application shell to Next.js in this RFC.

** Acceptance Criteria

1. Two workspace themes install together and top-level =theme= selects either.
2. Duplicate IDs and incompatible packages fail before bundling.
3. Variants switch without loading another structural theme.
4. A generated theme is a normal package passing the shared contract suite.
5. SPA and Next.js fixtures consume the same portable content and theme identity.
6. Production evidence excludes unselected themes and server-only client leaks.
7. The legacy =[theme].name= route is removed after migration.

** Scenario Benchmark RFC

*** Runtime Decision

Org Zhixing keeps =Rsbuild + React + TanStack Router + TanStack Query + Effect=
as its primary application runtime.  This stack already owns typed routes,
loaders, preload behavior, lazy chunks, static deployment, and the validated
performance boundaries in this repository.

For the primary React SPA adapter, a component declared =server= means it is pure
and contains no browser-only API.  The SPA adapter renders it as a universal
component during client startup.  A component declared =client= remains an
explicit interactive leaf.  This preserves server-ready theme contracts without
making Next.js or SSR the primary runtime.

Next.js remains an optional theme adapter fixture.  A future server requirement
may evaluate TanStack Start.  Neither is a prerequisite for Mobile support or
Scenario Benchmark work.

*** Benchmark Layers

Scenario Benchmark uses three complementary layers:

- Playwright owns browser scenarios, device emulation, touch, accessibility
  assertions, traces, and visual baselines.
- Lighthouse CI owns mobile browser audits, Web Vitals, resource summaries, DOM
  size, and accessibility/performance assertions.
- Vitest owns deterministic pure-function and projection microbenchmarks.

No layer substitutes for another.  The existing UI benchmark remains useful as
a source of bundle and lazy-boundary facts while scenario ownership moves into
the structured benchmark tree.

*** Repository Layout

#+begin_example
benchmarks/
  scenarios/
    blog-index.json
    article-zen.json
    gallery-heavy.json
    travel-heavy.json
    agenda-cockpit.json
    theme-minimal-mobile.json
  baselines/
    deterministic.json
    timing.json
  fixtures/
  reports/                  # gitignored
#+end_example

Every scenario fixes its route, content fixture, theme, variant, viewport or
device, interaction sequence, deterministic budgets, timing warning threshold,
and timing hard limit.

*** Hybrid Gate

Deterministic facts block CI immediately:

- JavaScript and CSS bytes;
- request and resource bytes/counts;
- DOM node count;
- selected and unselected theme import boundaries;
- lazy chunk boundaries;
- horizontal document overflow;
- minimum touch target size;
- console and page errors;
- serious accessibility failures.

Machine-sensitive timing metrics use Git-tracked baselines.  Normal p50/p95,
FCP, LCP, CLS, and TBT movement produces trend warnings.  A timing metric blocks
only when it exceeds a calibrated hard limit such as
=max(baseline * 1.35, baseline + fixedAllowance)=.

The initial hard limits are normative:

| Metric | Warning | Blocking limit |
|--------+---------+----------------|
| FCP | > 110 percent of baseline | =max(baseline * 1.35, baseline + 250ms)= |
| LCP | > 110 percent of baseline | =max(baseline * 1.35, baseline + 350ms)= |
| TBT | > 115 percent of baseline | =max(baseline * 1.40, baseline + 100ms)= |
| scenario duration p95 | > 115 percent of baseline | =max(baseline * 1.35, baseline + 500ms)= |
| action latency p95 | > 115 percent of baseline | =max(baseline * 1.40, baseline + 75ms)= |
| CLS | any increase above 0.02 | baseline + 0.05, capped at 0.10 |

Timing baselines are collected in the pinned CI container with the lockfile's
browser version, five cold runs, one discarded warm-up, and the median run as the
record.  A baseline expires after 90 days, a browser major upgrade, or a runner
class change.  Updating an expired or regressed baseline requires an explicit PR
command, the generated delta receipt, and reviewer approval; CI never updates it.

Scenario definitions, fixtures, deterministic baselines, timing baselines, and
necessary visual references are committed.  Raw traces, HTML reports, and
Lighthouse reports are CI artifacts and remain outside Git.

Baseline updates require an explicit command.  The command reports old value,
new value, and percentage change; ordinary test runs never rewrite baselines.

*** Device Matrix

| Project | Viewport | Primary role |
|---------------+----------+------------------------------------|
| compact-phone | 360x800 | minimum supported phone width |
| modern-phone | 390x844 | primary touch baseline |
| tablet | 768x1024 | tablet and split-layout transition |
| desktop | 1440x900 | non-Mobile comparison baseline |

Playwright projects also declare touch capability, mobile browser behavior,
orientation cases, color scheme, and reduced-motion cases where relevant.

The engine and platform matrix is:

| Project | Engine profile | Viewports | Cadence |
|---------+----------------+-----------+---------|
| chromium-android | Chromium with Android touch/mobile emulation | 360x800, 390x844 | every PR |
| webkit-ios | WebKit with iPhone touch/mobile emulation | 390x844 | every PR |
| chromium-tablet | Chromium touch tablet emulation | 768x1024 | every PR |
| chromium-desktop | Desktop Chromium | 1440x900 | every PR |
| webkit-tablet | WebKit tablet emulation | 768x1024 | nightly |
| firefox-responsive | Desktop Firefox responsive viewport | 390x844, 768x1024, 1440x900 | nightly |

Emulation is not represented as testing physical Android or iOS hardware.  A
release candidate additionally receives manual Safari iOS and Chrome Android
smoke verification, recorded as a release receipt.

** Mobile Support RFC

*** Dependency Decision

Mobile support reuses mature ecosystem owners:

- =@radix-ui/react-dialog= owns the Mobile navigation overlay, focus trap,
  Escape handling, Portal, inert background behavior, and accessible title and
  description semantics.
- Playwright owns device, touch, orientation, keyboard, and screenshot scenarios.
- Lighthouse CI owns mobile performance and accessibility audits.
- Native CSS owns responsive layout, container queries, dynamic viewport units,
  safe-area insets, coarse-pointer behavior, and reduced motion.

Org Zhixing does not implement its own dialog, focus manager, device emulator,
or performance auditor.

*** Responsive Ranges

| Range | Width |
|---------+---------------|
| compact | <= 480px |
| mobile | 481px-767px |
| tablet | 768px-1023px |
| desktop | >= 1024px |

Container queries control reusable view components.  Viewport media queries are
reserved for the application shell, safe-area behavior, and orientation.

*** Mobile Shell

The Mobile header contains one-line brand context, current view, and a menu
trigger.  Navigation is rendered in a Radix Dialog drawer.  Opening the drawer
locks background interaction and transfers focus; route selection closes it and
restores focus.  Interactive targets are at least =44x44px=.

The desktop navigation remains the existing structure.  Mobile support does not
load a second application or structural theme.

*** View Requirements

- Blog, Notes, and Memory render as a single readable column.
- Gallery and Travel use one or two adaptive columns with no document-level
  horizontal overflow.
- Agenda collapses its toolbar and presents secondary panels vertically or in a
  drawer.
- Zen reader progress avoids safe-area and navigation overlap.
- Long links wrap safely; tables and code blocks use local scrolling rather than
  widening the document.
- Input and capture views use dynamic viewport units so the software keyboard
  does not hide active controls.

The shell and every view support portrait, landscape, safe-area insets, coarse
pointer input, absent hover, =prefers-reduced-motion=, color-scheme preference,
keyboard navigation, screen readers, and 200 percent text scaling.

*** Mobile CSS Contract

The implementation uses =100dvh= where viewport height matters,
=env(safe-area-inset-*)= for shell padding, =touch-action: manipulation= on
activation controls, and scoped =overscroll-behavior= for drawers and local
scroll regions.  Safe-area values augment theme tokens rather than replacing
them.

*** Mobile Scenario Matrix

| Scenario | 360x800 | 390x844 | 768x1024 | 1440x900 |
|-------------------+---------+---------+----------+----------|
| Blog index | yes | yes | yes | yes |
| Zen article | yes | yes | yes | yes |
| Mobile navigation | yes | yes | yes | no |
| Gallery heavy | yes | yes | yes | yes |
| Travel heavy | yes | yes | yes | yes |
| Agenda cockpit | yes | yes | yes | yes |
| Theme and variant | yes | yes | yes | yes |

*** Delivery Order

1. Add the Scenario Benchmark schema, runner, fixtures, and Git baselines.
2. Add Playwright, Lighthouse CI, and Radix Dialog.
3. Implement the Mobile shell and navigation.
4. Repair Blog, Gallery, Travel, Agenda, and input surfaces against the matrix.
5. Generate and review the four viewport baselines.
6. Use browser visual assistance only at final engineering verification, then
   correct remaining visual defects.
