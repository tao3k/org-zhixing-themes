#+TITLE: Typst Rendering Performance
#+DATE: 2026-07-17
#+AUTHOR: Org Zhixing Maintainers
#+FILETAGS: :org-zhixing:operations:performance:typst:

* Scenario

This page exercises a representative mathematical document while the scenario
benchmark measures cold rendering and persistent-cache reload performance. The
single preview combines integrals, infinite series, determinants, matrices, and
vectors without duplicating reader-facing content.

#+begin_src typst
$
  integral_0^infinity e^(-alpha x^2) dif x
    &= sqrt(pi) / (2 sqrt(alpha)), quad sum_(n=1)^infinity 1/n^2 = pi^2 / 6 \
  det(mat(a, b; c, d))
    &= a d - b c, quad mat(1, 2; 3, 4) vec(x, y) = vec(x + 2 y, 3 x + 4 y)
$
#+end_src
